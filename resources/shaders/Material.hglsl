// Material header file

struct PhongMaterial
{
    vec3 ka;
	vec3 kd;
	vec4 ks; // ks.rgb + ns
};

struct CookTorranceMaterial
{
    vec3  albedo; float albedoPadding;
    float roughness;
    float metallic;
    float fresnel;
};