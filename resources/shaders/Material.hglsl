// Material header file

#define PHONG_MATERIAL        0
#define ALBEDO_MATERIAL       1
#define COOKTORRANCE_MATERIAL 2

struct PhongMaterial
{
    vec3 ka;
	vec3 kd;
	vec4 ks; // ks.rgb + ns
};

struct CookTorranceMaterial
{
    vec3  albedo; uint useDiffuseMap;

    float roughness;
    float metallic;
    float fresnel;
    uint useSpecularMap;
};